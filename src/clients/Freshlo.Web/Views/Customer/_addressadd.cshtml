@using Freshlo.Web.Models.CustomerVM
@using System.Globalization
@using Freshlo.Web.Helpers
@model DetailViewModel
@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
<table class="table table-striped-even ab-table-head-bg-warning-light table-borderless" id="Addaddresslist">
    <thead>
        <tr>
            <th>Customer Name</th>
            <th>Email</th>
            <th>Contact no</th>
            <th>Address</th>
            <th>Edit</th>ssss
            <th>Remove</th>

        </tr>
    </thead>
    <tbody id="addaddress">
        @{
            if (Model != null)
            {
                foreach (var item in @Model.CustomerDetailist)
                {
                    <tr>
                        <th>@item.Name</th>
                        <th>@item.EmailId</th>
                        <th>@item.ContactNo</th>
                        <th>@item.Address1</th>
                        <td><a href="#" class="text-dark" data-id="@item.AddId"> <i class="flaticon-edit"></i> </a></td>
                        <td><a href="#" class="text-danger deleteId" data-id="@item.AddId"> <i class="flaticon-delete deleteId"> </i></a></td>
                    </tr>
                }
            }
        }
    </tbody>
</table>
